import { RoadStatePipe } from './road-state.pipe';

describe('RoadStatePipe', () => {
  it('create an instance', () => {
    const pipe = new RoadStatePipe();
    expect(pipe).toBeTruthy();
  });
});
