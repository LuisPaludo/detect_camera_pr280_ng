import { RoadStatePastPipe } from './road-state-past.pipe';

describe('RoadStatePastPipe', () => {
  it('create an instance', () => {
    const pipe = new RoadStatePastPipe();
    expect(pipe).toBeTruthy();
  });
});
